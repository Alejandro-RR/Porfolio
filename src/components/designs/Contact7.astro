---
// Contact component - Diseño 7 (Divine/Roman Style)
---

<section class="contact" id="contact">
	<div class="container">
		<div class="section-header">
			<div class="section-icon">📿</div>
			<h2 class="section-title">Contacto</h2>
			<div class="section-underline"></div>
		</div>

		<div class="contact-temple">
			<div class="temple-columns">
				<div class="contact-info-column">
					<h3>Información de Contacto</h3>
					<div class="contact-items">
						<div class="contact-item-divine">
							<div class="item-icon">📧</div>
							<div class="item-content">
								<strong>Email</strong>
								<a href="mailto:alex@example.com">alex@example.com</a>
							</div>
						</div>
						<div class="contact-item-divine">
							<div class="item-icon">📱</div>
							<div class="item-content">
								<strong>Teléfono</strong>
								<a href="tel:+34612345678">+34 612 345 678</a>
							</div>
						</div>
						<div class="contact-item-divine">
							<div class="item-icon">📍</div>
							<div class="item-content">
								<strong>Ubicación</strong>
								<span>Barcelona, España</span>
							</div>
						</div>
					</div>
					<div class="social-altar">
						<h4>Redes Sociales</h4>
						<div class="social-buttons-divine">
							<a href="#" class="social-btn-divine">LinkedIn</a>
							<a href="#" class="social-btn-divine">GitHub</a>
							<a href="#" class="social-btn-divine">Twitter</a>
						</div>
					</div>
				</div>

				<div class="contact-form-column">
					<h3>Enviar Mensaje</h3>
					<form class="divine-form">
						<div class="form-group-divine">
							<label>Nombre</label>
							<input type="text" placeholder="Tu nombre" required />
						</div>
						<div class="form-group-divine">
							<label>Email</label>
							<input type="email" placeholder="tu@email.com" required />
						</div>
						<div class="form-group-divine">
							<label>Asunto</label>
							<input type="text" placeholder="Asunto del mensaje" required />
						</div>
						<div class="form-group-divine">
							<label>Mensaje</label>
							<textarea rows="5" placeholder="Escribe tu mensaje..." required></textarea>
						</div>
						<button type="submit" class="submit-divine">
							<span>⚜</span>
							<span>Enviar Mensaje</span>
						</button>
					</form>
				</div>
			</div>
		</div>
	</div>
</section>

<style>
	.contact {
		padding: 5rem 2rem;
		background: linear-gradient(135deg, #f5f1e8 0%, #faf8f3 100%);
		position: relative;
	}

	.contact::before {
		content: '';
		position: absolute;
		top: 0;
		left: 0;
		right: 0;
		bottom: 0;
		background-image: 
			repeating-linear-gradient(
				90deg,
				transparent,
				transparent 20px,
				rgba(212, 175, 55, 0.02) 20px,
				rgba(212, 175, 55, 0.02) 40px
			);
		pointer-events: none;
	}

	.container {
		max-width: 1200px;
		margin: 0 auto;
		position: relative;
		z-index: 1;
	}

	.section-header {
		text-align: center;
		margin-bottom: 3rem;
	}

	.section-icon {
		font-size: 3rem;
		color: #d4af37;
		margin-bottom: 1rem;
		text-shadow: 0 2px 8px rgba(212, 175, 55, 0.3);
	}

	.section-title {
		font-family: 'Georgia', 'Times New Roman', serif;
		font-size: 2.5rem;
		color: #2c1810;
		margin: 0 0 1rem 0;
		font-weight: 700;
		letter-spacing: 2px;
		text-transform: uppercase;
	}

	.section-underline {
		width: 150px;
		height: 3px;
		background: linear-gradient(90deg, transparent, #d4af37, transparent);
		margin: 0 auto;
		position: relative;
	}

	.section-underline::before,
	.section-underline::after {
		content: '⚜';
		position: absolute;
		top: 50%;
		transform: translateY(-50%);
		color: #d4af37;
		font-size: 1rem;
	}

	.section-underline::before {
		left: -30px;
	}

	.section-underline::after {
		right: -30px;
	}

	.contact-temple {
		background: rgba(255, 255, 255, 0.7);
		border: 3px solid #d4af37;
		border-radius: 8px;
		padding: 3rem;
		box-shadow: 
			0 0 0 2px rgba(212, 175, 55, 0.2),
			0 20px 60px rgba(0, 0, 0, 0.1),
			inset 0 0 50px rgba(255, 255, 255, 0.5);
		position: relative;
	}

	.contact-temple::before {
		content: '';
		position: absolute;
		top: -3px;
		left: -3px;
		right: -3px;
		bottom: -3px;
		background: linear-gradient(45deg, #d4af37, #f4d03f, #d4af37);
		border-radius: 8px;
		z-index: -1;
		opacity: 0.2;
	}

	.temple-columns {
		display: grid;
		grid-template-columns: 1fr 1.5fr;
		gap: 3rem;
	}

	h3 {
		font-family: 'Georgia', 'Times New Roman', serif;
		font-size: 1.5rem;
		color: #2c1810;
		margin: 0 0 2rem 0;
		font-weight: 700;
		letter-spacing: 1px;
		text-transform: uppercase;
		padding-bottom: 1rem;
		border-bottom: 2px solid rgba(212, 175, 55, 0.3);
	}

	.contact-items {
		display: flex;
		flex-direction: column;
		gap: 1.5rem;
		margin-bottom: 2rem;
	}

	.contact-item-divine {
		display: flex;
		align-items: center;
		gap: 1rem;
		padding: 1rem;
		background: rgba(212, 175, 55, 0.05);
		border-left: 4px solid #d4af37;
		border-radius: 4px;
	}

	.item-icon {
		font-size: 1.5rem;
		width: 50px;
		height: 50px;
		display: flex;
		align-items: center;
		justify-content: center;
		background: rgba(212, 175, 55, 0.1);
		border-radius: 50%;
		border: 2px solid #d4af37;
	}

	.item-content {
		flex: 1;
		display: flex;
		flex-direction: column;
		gap: 0.25rem;
	}

	.item-content strong {
		font-size: 0.85rem;
		color: #8b7355;
		text-transform: uppercase;
		letter-spacing: 1px;
		font-weight: 600;
	}

	.item-content a {
		color: #2c1810;
		text-decoration: none;
		font-weight: 600;
		font-size: 1rem;
		transition: color 0.3s ease;
	}

	.item-content a:hover {
		color: #d4af37;
	}

	.item-content span {
		color: #2c1810;
		font-weight: 600;
		font-size: 1rem;
	}

	.social-altar {
		padding-top: 2rem;
		border-top: 2px solid rgba(212, 175, 55, 0.3);
	}

	h4 {
		font-family: 'Georgia', 'Times New Roman', serif;
		font-size: 1rem;
		color: #2c1810;
		margin: 0 0 1rem 0;
		font-weight: 700;
		letter-spacing: 1px;
		text-transform: uppercase;
	}

	.social-buttons-divine {
		display: flex;
		flex-direction: column;
		gap: 0.75rem;
	}

	.social-btn-divine {
		padding: 0.875rem 1.5rem;
		background: rgba(212, 175, 55, 0.1);
		color: #2c1810;
		border: 2px solid #d4af37;
		border-radius: 6px;
		text-decoration: none;
		font-weight: 600;
		text-align: center;
		transition: all 0.3s ease;
		letter-spacing: 1px;
	}

	.social-btn-divine:hover {
		background: #d4af37;
		color: #2c1810;
		transform: translateY(-2px);
		box-shadow: 0 4px 15px rgba(212, 175, 55, 0.3);
	}

	.divine-form {
		display: flex;
		flex-direction: column;
		gap: 1.5rem;
	}

	.form-group-divine {
		display: flex;
		flex-direction: column;
		gap: 0.5rem;
	}

	.form-group-divine label {
		font-family: 'Georgia', 'Times New Roman', serif;
		font-size: 0.9rem;
		color: #2c1810;
		font-weight: 600;
		letter-spacing: 0.5px;
		text-transform: uppercase;
	}

	input,
	textarea {
		padding: 0.875rem;
		border: 2px solid #d4af37;
		border-radius: 6px;
		background: rgba(255, 255, 255, 0.9);
		color: #2c1810;
		font-size: 0.95rem;
		font-family: inherit;
		transition: all 0.3s ease;
	}

	input:focus,
	textarea:focus {
		outline: none;
		border-color: #f4d03f;
		box-shadow: 
			0 0 0 3px rgba(212, 175, 55, 0.2),
			0 4px 15px rgba(212, 175, 55, 0.1);
		background: #ffffff;
	}

	textarea {
		resize: vertical;
		min-height: 120px;
	}

	.submit-divine {
		padding: 1rem 2rem;
		background: linear-gradient(135deg, #d4af37 0%, #f4d03f 100%);
		color: #2c1810;
		border: 2px solid #d4af37;
		border-radius: 6px;
		font-weight: 700;
		font-size: 1rem;
		cursor: pointer;
		display: flex;
		align-items: center;
		justify-content: center;
		gap: 0.75rem;
		transition: all 0.3s ease;
		letter-spacing: 1px;
		text-transform: uppercase;
		font-family: 'Georgia', 'Times New Roman', serif;
		box-shadow: 
			0 4px 15px rgba(212, 175, 55, 0.4),
			inset 0 1px 0 rgba(255, 255, 255, 0.3);
	}

	.submit-divine:hover {
		background: linear-gradient(135deg, #f4d03f 0%, #d4af37 100%);
		transform: translateY(-2px);
		box-shadow: 
			0 6px 20px rgba(212, 175, 55, 0.5),
			inset 0 1px 0 rgba(255, 255, 255, 0.3);
	}

	.submit-divine span:first-child {
		font-size: 1.2rem;
	}

	@media (max-width: 968px) {
		.temple-columns {
			grid-template-columns: 1fr;
		}

		.contact-temple {
			padding: 2rem 1.5rem;
		}
	}
</style>

