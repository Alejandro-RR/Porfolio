<section class="hero" id="hero">
	<div class="hero-wrapper">
		<div class="hero-text">
			<p class="eyebrow">PORTAFOLIO</p>
			<h1>Construyo experiencias digitales claras y efectivas</h1>
			<p class="description">
				Desarrollador front-end enfocado en interfaces limpias, accesibles y rápidas.
				Trabajo con equipos para transformar ideas en productos digitales escalables.
			</p>
			<div class="hero-actions">
				<a href="#projects" class="btn primary">Ver proyectos</a>
				<a href="#contact" class="btn secondary">Contacto</a>
			</div>
		</div>
		<div class="hero-visual">
			<div class="card">
				<div class="card-header">
					<span class="dot"></span>
					<span class="dot"></span>
					<span class="dot"></span>
				</div>
				<div class="card-body">
					<p class="card-title">Último proyecto</p>
					<h3>Dashboard de métricas</h3>
					<p class="card-desc">Visualizaciones en tiempo real, filtros personalizados y experiencia responsive.</p>
					<div class="card-footer">
						<span>Tiempo</span>
						<strong>4 semanas</strong>
					</div>
				</div>
			</div>
		</div>
	</div>
</section>

<style>
	.hero {
		background: #f8fafc;
		padding: 6rem 1.5rem;
		padding-top: calc(6rem + 80px);
	}

	.hero-wrapper {
		max-width: 1100px;
		margin: 0 auto;
		display: grid;
		grid-template-columns: repeat(2, minmax(0, 1fr));
		gap: 4rem;
		align-items: center;
	}

	.hero-text h1 {
		font-size: 3rem;
		line-height: 1.1;
		color: #0f172a;
		margin-bottom: 1.5rem;
		font-weight: 700;
	}

	.eyebrow {
		font-size: 0.85rem;
		letter-spacing: 0.2rem;
		text-transform: uppercase;
		color: #2563eb;
		margin-bottom: 1rem;
		font-weight: 600;
	}

	.description {
		font-size: 1.1rem;
		color: #475569;
		line-height: 1.7;
		margin-bottom: 2rem;
		max-width: 520px;
	}

	.hero-actions {
		display: flex;
		gap: 1rem;
		flex-wrap: wrap;
	}

	.btn {
		padding: 0.9rem 1.8rem;
		border-radius: 999px;
		font-weight: 600;
		text-decoration: none;
		font-size: 0.95rem;
		transition: transform 0.2s ease, box-shadow 0.2s ease;
	}

	.btn.primary {
		background: #2563eb;
		color: #fff;
		box-shadow: 0 12px 30px rgba(37, 99, 235, 0.25);
	}

	.btn.primary:hover {
		transform: translateY(-2px);
		box-shadow: 0 18px 35px rgba(37, 99, 235, 0.25);
	}

	.btn.secondary {
		background: transparent;
		color: #2563eb;
		border: 1px solid #cbd5f5;
	}

	.btn.secondary:hover {
		transform: translateY(-2px);
		box-shadow: 0 12px 25px rgba(15, 23, 42, 0.08);
	}

	.hero-visual {
		display: flex;
		justify-content: center;
	}

	.card {
		background: #fff;
		border-radius: 1.5rem;
		box-shadow: 0 25px 60px rgba(15, 23, 42, 0.08);
		max-width: 360px;
		width: 100%;
	}

	.card-header {
		display: flex;
		gap: 0.5rem;
		padding: 1rem 1.5rem;
		border-bottom: 1px solid #e2e8f0;
	}

	.dot {
		width: 10px;
		height: 10px;
		border-radius: 50%;
		background: #e2e8f0;
	}

	.dot:nth-child(1) { background: #f97316; }
	.dot:nth-child(2) { background: #22c55e; }
	.dot:nth-child(3) { background: #2563eb; }

	.card-body {
		padding: 2rem 1.5rem 1.5rem;
		display: flex;
		flex-direction: column;
		gap: 1rem;
	}

	.card-title {
		font-size: 0.85rem;
		color: #94a3b8;
		text-transform: uppercase;
		letter-spacing: 0.15rem;
	}

	h3 {
		font-size: 1.5rem;
		color: #0f172a;
		margin: 0;
	}

	.card-desc {
		font-size: 0.95rem;
		color: #475569;
		line-height: 1.6;
	}

	.card-footer {
		display: flex;
		justify-content: space-between;
		align-items: center;
		padding-top: 1rem;
		border-top: 1px solid #e2e8f0;
		font-size: 0.9rem;
		color: #475569;
	}

	.card-footer strong {
		color: #0f172a;
		font-weight: 600;
	}

	@media (max-width: 968px) {
		.hero {
			padding: 4rem 1.25rem;
			padding-top: calc(4rem + 80px);
		}

		.hero-wrapper {
			grid-template-columns: 1fr;
			text-align: center;
			gap: 3rem;
		}

		.hero-text h1 {
			font-size: 2.25rem;
		}

		.description {
			margin: 0 auto 2rem;
		}

		.hero-visual {
			justify-content: center;
		}
	}
</style>
